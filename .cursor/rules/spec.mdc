---
description: อ่าน docs/SPEC.md ก่อนทำฟีเจอร์และกฎธุรกิจ
alwaysApply: true
---

# SPEC-first

แหล่งความจริงเดียว: [`docs/SPEC.md`](../../docs/SPEC.md)

ก่อนเขียนโค้ดที่เกี่ยวกับฟีเจอร์ สิทธิ์ หรือกฎธุรกิจ:

1. อ่าน `docs/SPEC.md` (ส่วนที่เกี่ยวกับงานนั้น)
2. อ้าง section ที่ใช้ตัดสินใจ
3. แล้วค่อย implement

- ห้ามเพิ่มฟีเจอร์นอก SPEC — ใส่ `TODO` แทน
- เฟสพัฒนา: Staff → Officer → Manager → Admin (ทำเฉพาะเฟสปัจจุบัน)
- งาน UI ล้วนไม่ต้องอ่านทั้งไฟล์ แต่ถ้าแตะ flow คำร้อง ปุ่ม หรือสิทธิ์ ต้องเช็ค SPEC ก่อน

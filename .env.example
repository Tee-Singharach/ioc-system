# คัดลอกเป็น .env แล้วใส่ค่าจริง (ห้าม commit .env)
# รหัสผ่านที่มี @ # / ? ^ ~ ฯลฯ ต้อง URL-encode ก่อนใส่ใน connection string
# ชื่อ DB บน Plesk มักเป็น prefix_user เช่น solinex_ioc_system (ไม่ใช่แค่ ioc_system)
DATABASE_URL="mysql://USER:PASSWORD@HOST:3306/solinex_ioc_system"
